const REQUEST_VALIDATION_TARGETS = require('./request-validation-targets');
const DB_MODELS = require('./db-models');
const LIKE_TYPES = require('./like-types');

module.exports = {
    REQUEST_VALIDATION_TARGETS,
    DB_MODELS,
    LIKE_TYPES,
};
